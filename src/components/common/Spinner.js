import React from "react";

const Spinner = () => {
    return (
       <div className="spinner-container inline-block">
           <div className="spin inline-block"/>
       </div>

    )
};

export default Spinner;